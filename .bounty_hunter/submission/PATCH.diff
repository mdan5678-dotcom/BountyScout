diff --git a/scout_bounties.py b/scout_bounties.py
index 35add25..e7191ca 100644
--- a/scout_bounties.py
+++ b/scout_bounties.py
@@ -17,6 +17,11 @@ SEARCH_QUERIES = [
     'is:issue is:open "Opire" bounty sort:updated-desc',
 ]
 
+def format_opportunity_word(count, capitalize=False):
+    """Return the correctly singular or plural form of opportunity."""
+    word = "opportunity" if count == 1 else "opportunities"
+    return word.capitalize() if capitalize else word
+
 def load_seen_bounties():
     """Load previously seen bounty URLs from the state file."""
     if os.path.exists(STATE_FILE):
@@ -183,11 +188,13 @@ def main():
 
     # Format notification message
     now_str = datetime.now(timezone.utc).strftime("%Y-%m-%d %H:%M UTC")
+    opportunity_word = format_opportunity_word(len(new_bounties))
+    title_opportunity_word = format_opportunity_word(len(new_bounties), capitalize=True)
     
     # 1. Telegram / Discord Message Format (Markdown)
     notif_lines = [
         f"🎯 *New Bounty Alert* ({now_str})",
-        f"Found {len(new_bounties)} new opportunity{'ies' if len(new_bounties) > 1 else ''}:\n"
+        f"Found {len(new_bounties)} new {opportunity_word}:\n"
     ]
     for idx, b in enumerate(new_bounties, start=1):
         notif_lines.append(f"{idx}. *{b['title']}*")
@@ -211,7 +218,7 @@ def main():
 
     # Method C: GitHub Issue (Built-in, zero configuration)
     if github_token and repo_fullname:
-        issue_title = f"🎯 Bounty Alert: {len(new_bounties)} New Opportunity{'ies' if len(new_bounties) > 1 else ''} found"
+        issue_title = f"🎯 Bounty Alert: {len(new_bounties)} New {title_opportunity_word} found"
         issue_body = (
             f"### Active Bounty Scan Results\n\n"
             f"**Scan Time:** {now_str}\n\n"
